import {notFound} from 'next/navigation';import type {Metadata} from 'next';import {Header,Footer,CTA} from '../../components';import {researchPosts,site} from '../../data';export function generateStaticParams(){return researchPosts.map(p=>({slug:p.slug}))}export async function generateMetadata({params}:{params:Promise<{slug:string}>}):Promise<Metadata>{const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)return {};return {title:p.title,description:p.excerpt,alternates:{canonical:`https://${site.domain.toLowerCase()}/research/${p.slug}`}}}export default async function ResearchPost({params}:{params:Promise<{slug:string}>}){const {slug}=await params;const p=researchPosts.find(x=>x.slug===slug);if(!p)notFound();const structuredData={'@context':'https://schema.org','@type':'Article',headline:p.title,datePublished:p.published,dateModified:p.published,mainEntityOfPage:`https://${site.domain.toLowerCase()}/research/${p.slug}`};return <><Header/><main className="section"><article className="container guide-article"><script type="application/ld+json" dangerouslySetInnerHTML={{__html:JSON.stringify(structuredData)}}/><p className="eyebrow">Philippines staffing research</p><h1>{p.title}</h1><p className="lead">{p.excerpt}</p><p className="article-meta"><time dateTime={p.published}>Published {p.published}</time> · Evidence-first research</p><img className="sa-booking-image" src="/thank-you-hero.png" alt="Outsourced callers research workflow" width="619" height="402" /><div className="card">{p.body.map(x=><p key={x}>{x.startsWith('Sources:')?<><strong>Sources:</strong> <a href="https://www.nist.gov/cyberframework" rel="noopener noreferrer" target="_blank">NIST CSF 2.0</a>, <a href="https://www.ftc.gov/business-guidance/privacy-security/data-security" rel="noopener noreferrer" target="_blank">FTC data security guidance</a>, and <a href="https://www.fcc.gov/general/telemarketing-and-telephone-consumer-protection-act" rel="noopener noreferrer" target="_blank">FCC telemarketing guidance</a>.</>:x}</p>)}<p><a href="/contact">Discuss a controlled Philippines calling workflow →</a></p></div></article><CTA/></main><Footer/></>}
